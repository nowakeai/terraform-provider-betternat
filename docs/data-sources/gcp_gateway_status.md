# betternat_gcp_gateway_status

`betternat_gcp_gateway_status` is reserved for the GCP alpha surface.

The data source currently returns a not-implemented diagnostic because GCP
route replacement, gateway forwarding, and coordination behavior still need
disposable-environment validation before BetterNAT exposes a working GCP
provider resource.
