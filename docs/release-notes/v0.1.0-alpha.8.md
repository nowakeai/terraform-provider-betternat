# terraform-provider-betternat v0.1.0-alpha.8 Release Notes

Date: 2026-06-24

## Status

`v0.1.0-alpha.8` is an alpha Terraform provider release for BetterNAT. It
updates the provider runtime support matrix so Terraform installs can use the
current BetterNAT runtime artifact set:

```hcl
betternat_version = "v0.1.0-alpha.6"
```

This release is still an alpha technical preview. Test in a disposable or
non-critical AWS environment before using it with real private route tables.

## What Changed Since v0.1.0-alpha.7

- Added support for BetterNAT runtime `v0.1.0-alpha.6`.
- Kept support for BetterNAT runtime `v0.1.0-alpha.2`.
- Updated provider docs and examples to recommend runtime
  `v0.1.0-alpha.6`.
- Updated the provider dependency to BetterNAT main module
  `v0.1.0-alpha.7`, which contains the runtime artifact support matrix change.

## Example

```hcl
terraform {
  required_providers {
    betternat = {
      source  = "nowakeai/betternat"
      version = "= 0.1.0-alpha.8"
    }
  }
}

resource "betternat_gateway" "egress" {
  name   = "prod-egress-a"
  region = "us-west-2"
  vpc_id = aws_vpc.main.id

  public_subnet_ids = {
    "us-west-2a" = aws_subnet.public_a.id
  }

  private_route_table_ids = {
    "us-west-2a" = [aws_route_table.private_a.id]
  }

  private_cidrs = [aws_vpc.main.cidr_block]

  ami_id            = data.aws_ami.al2023_arm64.id
  betternat_version = "v0.1.0-alpha.6"
  stable_egress_ip  = true
}
```

## Validation Before Publication

- BetterNAT main repository `GOCACHE=$PWD/tmp/go-build go test ./...`: passed.
- BetterNAT main repository provider build: passed.
- Split provider `go test ./...`: passed locally before release.
- Split provider build: passed locally before release.

Public Terraform Registry install and AWS soak validation are tracked in the GA
release checklist after this provider release is published and synced.

## Known Limitations

- AWS only.
- Single-AZ HA group only.
- No NAT Gateway equivalent SLA.
- No active connection preservation.
- No public BetterNAT AMI is published.
- `ami_channel` resolution is not implemented.
- OpenTofu Registry propagation can lag Terraform Registry propagation.
- Updating `betternat_version` changes the launch template and bootstrap
  metadata, but existing gateway nodes must still be replaced or refreshed to
  prove the running binaries changed.

## Artifact Integrity

Verify downloads with the attached provider `SHA256SUMS` file and signature.

This release note is not legal advice.
